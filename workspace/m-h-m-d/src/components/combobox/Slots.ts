export interface coreSlots {
    input(): any,
    item(props: { isActive: boolean, item: string }): any
}
export interface uiSlots {
    input(): any,
    item(props: { isActive: boolean, item: string }): any
}