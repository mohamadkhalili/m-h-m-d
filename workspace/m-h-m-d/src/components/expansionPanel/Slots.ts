export interface coreSlots {
    title(props: { isActive: boolean}): any,
    text(props: { isActive: boolean}): any
}
export interface uiSlots {
    title(props: { isActive: boolean}): any,
    text(props: { isActive: boolean}): any
}